# 🚀 Environment Configuration Template
# Copy this file to .env.local and update the values

# ========================
# 🌐 API Configuration
# ========================
VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_NAME=React Boilerplate

# ========================
# 🔧 Feature Flags  
# ========================
# Enable/disable optimistic updates
VITE_ENABLE_OPTIMISTIC_UPDATES=true

# Enable/disable prefetching strategies
VITE_ENABLE_PREFETCHING=true

# Enable/disable React Query DevTools in development
VITE_ENABLE_QUERY_DEVTOOLS=true

# ========================
# 🐛 Development Settings
# ========================
# Enable detailed error logging
VITE_ENABLE_ERROR_LOGGING=true

# ========================
# 🔐 Persisted-State Encryption
# ========================
# AES key used by redux-persist-transform-encrypt to encrypt auth state
# in localStorage. If unset, auth state will NOT be persisted (fail-closed).
# Note: keys shipped in a browser bundle are not truly secret — this is
# obfuscation, not server-grade security. Rotate this on every release.
VITE_PERSIST_ENCRYPT_KEY=change-me-to-a-long-random-string

# ========================
# 📊 Analytics (Optional)
# ========================
# Sentry DSN for error tracking
VITE_SENTRY_DSN=your_sentry_dsn_here

# Google Analytics tracking ID
# VITE_GA_TRACKING_ID=GA_MEASUREMENT_ID

# ========================
# 📝 Notes
# ========================
# - All VITE_ prefixed variables are available in your React app
# - Never commit sensitive data like API keys or secrets
# - Use different values for development, staging, and production
# - Check the main README.md for more configuration options
